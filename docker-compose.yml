version: '1'

services:
  mysql-is_my_blog:
    image: mysql:latest
    environment:
      MYSQL_DATABASE: proiect_tsbdts
      MYSQL_USER: user
      MYSQL_PASSWORD: user
      MYSQL_ROOT_PASSWORD: user
    ports:
      - 3306:3306
    volumes:
      - ./dbdata:/var/lib/mysql